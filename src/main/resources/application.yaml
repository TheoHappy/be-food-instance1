eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_HOST:http://localhost:5000/eureka/}
  instance:
    instance-id: food-backend-instance-1
server:
  port: 5000
spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://fooddb1.ca9ycjopwtqs.us-east-2.rds.amazonaws.com:5432/food1
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
