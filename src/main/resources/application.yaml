eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_HOST:http://discovery-service-food-app.herokuapp.com:49226/eureka/}
  instance:
    instance-id: food-backend-instance-1
server:
  port: 8080
spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://fooddb1.ca9ycjopwtqs.us-east-2.rds.amazonaws.com:5432/food1
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
